import React from 'react';
import {{titleCase name}} from './{{titleCase name}}'

type {{titleCase name}}ContainerProps = {

}

const {{titleCase name}}Container: React.FC<{{titleCase name}}ContainerProps> = () => {
	return (
	<{{titleCase name}} />
	);
	};

	export default {{titleCase name}}Container;
